/** @type { import('next').NextConfig } */

const nextConfig = {
  /* config options here */
  // output: "export",
  basePath: '/lynus-website',
  assetPrefix: '/lynus-website',
};

export default nextConfig;
